(information)
[default value]
Client:
	Startup:
		Load AppConfig:
			SyncFolderPath (Which folder is synced)
			AuthToken (For safe server login)
			ServerAddress (Where the server is)
			
		Load UserConfig:
			CheckUpdate [false] (Whether the Application will check for updates)
			InstallUpdate [false] (Whether the application will automatically install any updates it finds. If false, it will instead notify the user)
			Silent (


Server: